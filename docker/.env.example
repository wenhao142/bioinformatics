# Default credentials for local, free-only stack.
# Copy to docker/.env and adjust as needed. Do NOT use in production.

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=omics

# Optional: change ports if conflicts
# MINIO_PORT=9000
# MINIO_CONSOLE_PORT=9001
# POSTGRES_PORT=5432
# OPENSEARCH_PORT=9200
